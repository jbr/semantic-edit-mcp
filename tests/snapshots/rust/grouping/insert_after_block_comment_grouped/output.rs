mod tests {
    /** Alpha doc. */
    #[test]
    fn alpha() {
        assert!(true);
    }
    /** Gamma doc. */
    #[test]
    fn gamma() {
        assert!(true);
    }

    /** Beta doc. */
    #[test]
    fn beta() {
        assert!(true);
    }
}
