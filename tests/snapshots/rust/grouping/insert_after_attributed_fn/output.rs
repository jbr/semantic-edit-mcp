mod tests {
    /// First test.
    #[test]
    fn first() {
        assert!(true);
    }
    /// Inserted test.
    #[test]
    fn inserted() {
        assert!(true);
    }

    /// Second test.
    #[test]
    fn second() {
        assert!(true);
    }
}
