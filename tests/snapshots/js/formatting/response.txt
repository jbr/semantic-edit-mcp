=== snapshot test tool call: stage_operation ===
STAGED: insert after

===DIFF===
     }
   }
   getUserById(id) {
+    console.log("here");
+  }
+  getUserById(id) {
     if (this.users.has(id)) {
       return this.users.get(id);
     }
