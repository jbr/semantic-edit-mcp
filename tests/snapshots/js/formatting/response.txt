=== snapshot test tool call: preview_edit ===
STAGED: insert after

===DIFF===
     }
   }
   getUserById(id) {
+    console.log("here");
+  }
+  getUserById(id) {
     if (this.users.has(id)) {
       return this.users.get(id);
     }
